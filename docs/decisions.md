# 初期設計判断

## Next.js App Router

Vercelへのデプロイとmetadata、OGP、将来のAPI追加を一つの構成で扱いやすいため採用する。

## ローカル状態のみ

MVPでは回答保存を行わず、主要体験の検証を優先する。DBと認証は利用状況を確認してから追加する。

## 外部状態管理なし

画面状態が小さいためReactのローカル状態で十分であり、依存関係と学習コストを増やさない。

## 固定選択率

最初から実回答集計を導入すると母数補正・不正対策・保存基盤が必要になるため、MVPでは固定データを使用する。
