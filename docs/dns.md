# DNS設定確認項目

- Vercel Project Settingsへ`reardore.ivrm.jp`を追加する
- Vercelが表示するDNS値を優先する
- `ivrm.jp`側で`reardore`サブドメインのレコードを設定する
- 既存の同名A/CNAMEレコードと競合していないことを確認する
- DNS反映後にValid ConfigurationとTLS発行を確認する
