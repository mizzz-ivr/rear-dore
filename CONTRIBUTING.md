# 開発参加手順

## ブランチ

- `main`: 本番公開可能な状態
- 機能追加: `feature/<内容>`
- 不具合修正: `fix/<内容>`

ブランチ名に`codex`を含めないでください。

## コミット・PR

コミットメッセージとPR本文は日本語で記載します。変更目的、影響範囲、確認方法、テスト内容を明記してください。

## 品質確認

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

無関係なリファクタや整形を同じPRへ混ぜず、小さくレビュー可能な差分を維持してください。
