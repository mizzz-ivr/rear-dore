# AGENTS.md

## プロジェクトの目的

「レアどれ？」は、みんなが選ばなそうな選択肢を予想し、少数派ほど高得点になるWebゲームです。

## 開発ルール

- PR本文、コミットメッセージ、レビュー向け説明、運用ドキュメントは日本語で記載する
- ブランチ名に`codex`を含めない
- 既存の責務分離と命名を尊重する
- 無関係なリファクタ、不要なリネーム、不要な整形を混ぜない
- 変更差分を小さくし、レビューしやすさを優先する
- 要件が曖昧な場合は、重要な仕様を断定せず仮定または要確認として扱う

## MVP範囲

- 今日の5問
- 4〜5択
- 回答後の分布表示
- レア度と得点
- 総合結果
- 結果共有

認証、DB保存、管理画面、ランキング、課金はMVP対象外です。

## 実装方針

- ゲームルールとスコア計算はUIから分離する
- 回答前に分布情報を表示しない
- クライアントで保持する状態を最小限にする
- 見た目のためだけに新しい依存関係を追加しない
- スマートフォンでの操作性とキーボード操作を維持する
- `prefers-reduced-motion`を尊重する

## テスト方針

変更時は最低限、以下を確認する。

- 正常系
- 異常系
- 境界値
- 既存ゲーム進行への影響
- モバイル表示
- キーボード操作
- metadataと本番URL

スコア、レア度、称号などのルールは純粋関数として単体テストを追加する。

## Vercel・Workflow変更時

- 本番URLは`https://reardore.ivrm.jp`
- Production Branchは`main`
- Preview URLをcanonical URLとして使用しない
- Vercel Hobby無料枠で不要なFunction実行やCronを増やさない
- Workflow変更後はlint、型チェック、テスト、ビルドがすべて通ることを確認する
